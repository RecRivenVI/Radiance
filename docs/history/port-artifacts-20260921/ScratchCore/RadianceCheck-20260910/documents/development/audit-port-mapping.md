# Radiance 当前实现与目标映射

> 当前项目名称统一为 Radiance。本文保留 2026-09-10 审计快照、结论和历史引用；其中旧称谓仅说明当时语境，不定义当前身份。后续工程与历史整合见 [当前工程记录](migration-template-configuration.md)。

审计范围：当前工作树（HEAD `9fe7811cf8b27f97b8b77307e982e3cc3942aee5` 加 dirty/untracked），只读对照 Radiance `414d8e330a2fc6cb1e8630cc95f2302b2b97a0e8` 与 MCVR `9905c81b1999f5845bf66d13501d371c16adf561`。Radiance 上游候选是 port `cd67362` 的父，MCVR 候选是 port `b611d24` 的父；两个父提交之间没有本审计已发现的显式 cross-repo lock。开场封存 `D:\Workspaces\Artifacts\OldPortAudit\20260910\Radiance-start.json` 的 SHA-256 为 `713715eb775c3a803a30cf2147b81d46cf01060d935da54cb76ef07991d304c5`；主任务另有 start-v2 manifest，二者不混称。

本记录只覆盖真实业务 Target `1.21.1-neoforge` 的入口、Mixin/资源/源码输入及其世界提交到 native/shader 的静态路径；`1.20.1-forge`、`26.1.2-neoforge`、`26.2-fabric`、`26.2-neoforge` 仅按当前目标事实标为治理节点，不把目录注册当作业务实现。没有启动游戏、GUI、构建或运行时验收，因此“静态已确认”不等于“运行已验证”。

状态词：`定位`=找到对应输入/接入点；`部分`=能力只覆盖一部分或已改变语义；`接入`=当前存在跨层调用；`尚未找到对应`=在本项列明的迁移/替代/注册/资源搜索范围内尚未定位对应项，不据此断言能力缺失；`确认缺失`仅在穷尽该能力作用域的迁移、替代、注册、调用和资源构建路径后使用；`静态已确认`=源码/资源直接证据；`运行待验`=仍需真实客户端确认。每项均列上游符号（U）与当前符号（C），并把行为改变和其原因证据分开。

## M-01 Target 矩阵与实际实现边界

- 两边路径符号：U `414d...:gradle.properties`、U `414d...:src/main/java/com/radiance/**`、U `414d...:src/main/resources/fabric.mod.json`；C `settings.gradle.kts:17-22`、C `versions/*/target.properties`、C `versions/1.21.1-neoforge/src/**`。
- 适用 Target：全注册矩阵；业务实现仅 `1.21.1-neoforge`。
- 变化类型：结构改名 + Minecraft `1.21.4 -> 1.21.1` + Fabric -> NeoForge；其他四节点治理占位。
- 行为直接证据：C `settings.gradle.kts:19-22` 注册五个 `:version:<target>`；C `versions/1.21.1-neoforge/target.properties:1-12` 为 `target.business=true`、`target.client=true`；C 其余四个 `target.properties` 的 `target.business/client/server=false`（例如 `versions/26.2-fabric/target.properties:1-10`）。C `versions/1.21.1-neoforge/src` 是唯一有 Java/资源输入的业务 Target。
- 原因证据：仓库 `AGENTS.md` 明确规定只有 `1.21.1-neoforge` 实现；这是治理约束，不是上游移植行为的历史原因。未找到 port 提交说明解释为何从 `1.21.4` 降至 `1.21.1`。
- 风险/缺证：不可从五节点注册推导其他四节点拥有产品行为；未对其执行业务入口、Mixin、资源或 native 等价审计。状态：`定位 / 静态已确认`。

## M-02 Mod 入口与 loader 接线

- 两边路径符号：U `414d...:src/main/resources/fabric.mod.json:15-34`、U `414d...:src/main/java/com/radiance/Radiance.java`、U `414d...:src/main/java/com/radiance/client/RadianceClient.java`；C `versions/1.21.1-neoforge/src/main/resources/META-INF/neoforge.mods.toml:1-33`、C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/Radiance.java:1-9`、C `.../platform/neoforge/RadianceNeoForge.java:1-14`。
- 适用 Target：`1.21.1-neoforge`。
- 变化类型：Fabric entrypoints -> NeoForge `@Mod(Dist.CLIENT)`；包名同步改名。
- 行为直接证据：U metadata 声明 client `com.radiance.client.RadianceClient` 与 main `com.radiance.Radiance`；C NeoForge metadata 声明 `javafml`、`[[mixins]] config="radiance.mixins.json"` 及 client-side Minecraft/NeoForge 依赖（C metadata:18-33），C `RadianceNeoForge` 构造器直接调用 `RadianceClient.initialize()`（C `RadianceNeoForge.java:8-13`）。C `RadianceClient.initializeInternal()` 从游戏目录复制 native/shaders/modules 并调用 `RendererProxy.initFolderPath`/`Pipeline`（C `client/RadianceClient.java:42-98`）。
- 原因证据：当前代码注释/AGENTS 只支持 NeoForge Early Window 和 Java 21；没有 port 历史记录解释入口选择的每个差异。
- 风险/缺证：真实 NeoForge mod discovery、classloading 顺序和 client-only 启动未运行验证；当前 loader metadata 不代表 GUI/客户端通过。状态：`接入 / 静态已确认 / 运行待验`。

## M-03 包、源码树和 Mixin 配置结构改名

- 两边路径符号：U `414d...:src/main/java/com/radiance/**`、U `414d...:src/main/resources/radiance.mixins.json`；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/**`、C `versions/1.21.1-neoforge/src/main/resources/radiance.mixins.json:1-107`。
- 适用 Target：当前业务 Target。
- 变化类型：物理路径重定位 `src/main` -> `versions/1.21.1-neoforge/src/main`；包 `com.radiance` -> `io.github.recrivenvi.radiance`；Mixin JSON package/plugin 同步改名。
- 行为直接证据：C Mixin JSON `package` 为 `io.github.recrivenvi.radiance.mixins`、`plugin` 为 `io.github.recrivenvi.radiance.mixin_related.MixinPlugin`（C JSON:4-6）；C 头部三个 common mixin 加 client 列表均采用新前缀（C JSON:7-107）。只读逐项核对 C JSON 注册项与 `mixins/**/*.java`：注册 107 项、Java mixin 107 项，`missingSource=0`、`unregistered=0`；相对 U 候选注册 69 项，C 尚未找到对应的 U 注册项包括 23 个 compatibility mixin 与 18 个 NeoForge/语义扩展，U 侧 `BillboardParticleMixins`、`ClientChunkManagerMixins`、`CloudRendererMixins` 在 C JSON 中尚未找到对应。native JNI 文件名也采用 `io_github_recrivenvi_radiance_*`（C `components/vulkan-renderer/src/core/middleware/io_github_recrivenvi_radiance_client_proxy_world_ChunkProxy.cpp:1,9`）。
- 原因证据：AGENTS 规定 Target 物理目录与 `io.github.recrivenvi` 实际 JNI 目标；未找到 upstream/port 说明解释该包名选择。
- 风险/缺证：源码路径、Mixin JSON、生成 JNI 头和 native 导出符号必须四者一致；本项仅静态确认字符串/路径一致，不证明生成头和加载器实际绑定。状态：`定位 / 静态已确认 / 运行待验`。

## M-04 Terrain 世界捕获：SectionCompiler -> chunk JNI -> BLAS/material

- 两边路径符号：U `414d...:src/main/java/com/radiance/mixins/vulkan_render_integration/ChunkBuilderMixins.java`、U `.../ChunkBuilderBuiltChunkMixins.java`、U `.../BuiltChunkStorageMixins.java`、U `.../client/proxy/world/ChunkProxy.java`；U MCVR `9905...:src/core/middleware/com_radiance_client_proxy_world_ChunkProxy.cpp`、`src/core/render/chunks.{hpp,cpp}`；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/mixins/vulkan_render_integration/{ChunkBuilderMixins,ChunkBuilderBuiltChunkMixins,BuiltChunkStorageMixins,SectionBuilderMixins}.java`、C `.../client/proxy/world/ChunkProxy.java`、C MCVR-imported `components/vulkan-renderer/src/core/middleware/io_github_recrivenvi_radiance_client_proxy_world_ChunkProxy.cpp`、`src/core/render/chunks.{hpp,cpp}`。
- 适用 Target：`1.21.1-neoforge`；native target `1.21.1-neoforge`。
- 变化类型：Minecraft/Fabric API -> Mojang/NeoForge API；主动捕获逻辑保留并扩展；native JNI 命名/异常包装；外部 section/NeoForge additional geometry 为本地新增。
- 行为直接证据：C `SectionBuilderMixins.java:54-139` 在 `SectionCompiler.compile(...)` HEAD 自建 `SectionCompiler.Results`，遍历方块/流体/模型，使用 `PBRVertexConsumer`，并在 `122-124` 调用 `ClientHooks.addAdditionalGeometry`；C `ChunkBuilderBuiltChunkMixins.java:37-63` 在 section reset/dirty 时入队并在 origin 变化时调用 `ChunkProxy.relocateSingle`；C `BuiltChunkStorageMixins.java:29-49` 在 storage 建立/释放和 camera reposition 时调用 `ChunkProxy.init/updateSectionPos`。C `ChunkProxy.java:93-107,613-644` 将 geometry 类型、group、texture、vertex format/count/address、important、`collectEmission` 传入 native。C native `...ChunkProxy.cpp:9-64` 将这些指针转成 `ChunkBuildTask` 并调用 `world->chunks()->queueChunkBuild`；C `chunks.cpp:1448-1537` 读取顶点、构造索引、建立/排队 BLAS 并上传 packed position/material/light buffers。
- 原因证据：C `SectionBuilderMixins.java:122-124` 的 `ClientHooks` 是 NeoForge 接入直接证据；C `ChunkProxy.java:139-142` 说明 vanilla visible-section/F3 计数被绕过，`ChunkProxy.java:294-339` 说明 additional renderer 与 external section 的主动分支。没有找到 port 记录解释为何改变 vanilla culling/rebuild 生命周期；这些注释只能证明意图，不能证明行为等价。
- 风险/缺证：U 采用 Fabric `ChunkBuilder`/`BuiltChunkStorage`，C 采用 `SectionRenderDispatcher`/`ViewArea`；`RenderRegionCache`、section readiness、additional renderer 的线程/生命周期只做静态检查。native build/CTest 与 Minecraft section rebuild 均未在本子任务运行。状态：`接入 / 静态已确认 / 运行待验`。

## M-05 Entity 世界捕获：render dispatcher -> layer/material arrays -> native entity batch

- 两边路径符号：U `414d...:src/main/java/com/radiance/client/proxy/world/EntityProxy.java:262-397,399-630,636-714,810-1100`；U MCVR `9905...:src/core/middleware/com_radiance_client_proxy_world_EntityProxy.cpp`、`src/core/render/entities.{hpp,cpp}`；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/client/proxy/world/EntityProxy.java:282-573,654-876,882-1001,1004-1148,1199-1312,1881-1903`；C `components/vulkan-renderer/src/core/middleware/io_github_recrivenvi_radiance_client_proxy_world_EntityProxy.cpp:7-63`、`src/core/render/entities.{hpp,cpp}:472-1182`。
- 适用 Target：`1.21.1-neoforge`。
- 变化类型：API/命名迁移；实体、block entity、crumbling、hand、particle、debug、weather 均改为主动 storage capture；当前新增层级路由与内容名语义。
- 行为直接证据：C `EntityProxy.java:282-338` 为每个实体建立 storage provider/outline provider；`392-400` 调用 `EntityRenderDispatcher.render`；`495-565` 将 camera-relative、name tag、outline、本体按 `RayTracingFlags` 加入 `EntityRenderDataList`；`570-573` 调用 `queueBuild`。C `EntityProxy.java:1608-1822` 分配实体/层级/geometry/name/texture/format/count/address 数组并调用 native `queueBuild`；C native `EntityProxy.cpp:7-57` 映射为 `EntitiesBuildTask`；C `entities.cpp:472-503` 解析实体数组，`323-362` 为非预建实例建立 BLAS，`1148-1180` 创建 `EntityBuildData`，最终 C `entities.cpp:1184+` 构建 batch。
- 原因证据：C `EntityProxy.java:112-117` 解释 CUSTOM 粒子 capture stack；`366-380` 解释 TextDisplay 两个 RenderType 家族；`575-592` 解释 camera entity 的 visibility gate。这些是当前实现者注释，未见上游 commit rationale。U 的基础实体 capture 与 C 的数组/JNI 结构有直接谱系对应，但 U 的 post-render 设计见 M-07。
- 风险/缺证：C 对 `RenderType.Mode` 额外接受 TRIANGLES/DEBUG_LINES/DEBUG_LINE_STRIP，native 对异常 line width/zero-alpha separator 有防护；这些主动行为不是 U 的直接等价移植。没有运行验证 renderer dispatcher 对所有实体/层级的覆盖，也没有证明 storage provider 关闭时机与 GPU fence 安全。状态：`接入 / 静态已确认 / 运行待验`。

## M-06 相机实体、本体自身可见性和反射路径

- 两边路径符号：U `414d...:src/main/java/com/radiance/mixins/vulkan_render_integration/WorldRendererMixins.java:163-177,200-333`、U `.../client/proxy/world/EntityProxy.java:366-393`、U MCVR `9905...:src/common/mapping.hpp:29-36` 与 `src/shader/world/ray_tracing/internal/*/world/world.rgen:434-488`；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/mixins/vulkan_render_integration/WorldRendererMixins.java:252-280`、C `.../client/proxy/world/EntityProxy.java:316-323,538-565,575-592`、C `components/vulkan-renderer/src/common/mapping.hpp:29-36`、C shader `.../world/world.rgen:434-485`。
- 适用 Target：`1.21.1-neoforge`；`vanilla-pt` 与 `advanced` 两个内置 shader pack。
- 变化类型：实体筛选由 Fabric redirect 迁移为显式 NeoForge world loop；bit 语义重排；attached awake camera entity primary-hidden、secondary-visible 的本地行为。
- 行为直接证据：U `WorldRendererMixins.java:163-177` 只在第一人称把 `Camera.isThirdPerson()` 重定向为 true，并放宽 48-block 内实体过滤；U `EntityProxy.java:366-374` 将 camera entity 标为 `PLAYER`。C `WorldRendererMixins.java:252-280` 显式遍历 `entitiesForRendering()`，对 camera entity 不丢弃（`255-271`），C `EntityProxy.java:320-323` 为 camera entity 计算 world/priority visibility，`538-546` 以 `PLAYER` 标记本体且 `reflect=true`。C `mapping.hpp:29-36` 将 bit 4 定义为 `PRIORITY_MASK`；C `world.rgen:434-485` primary mask 在 first person 排除 `PLAYER_MASK`，secondary mask 包含 `PLAYER_MASK`/`PRIORITY_MASK`。C priority `background.rgen:26-28`、`priority.rgen:26-29` 在非第一人称才并入 `PLAYER_MASK`。
- 原因证据：C `WorldRendererMixins.java:255-258` 明确注释“需要 camera entity geometry 供 PLAYER rays，primary 仍隐藏”；C `EntityProxy.java:575-592` 说明 attached/awake、detached、sleeping 三态；port 历史 `cd67362:SOURCE_FIRST_COMPATIBILITY_AUDIT.md:801-813` 记录了同一套 attached/sleeping/detached 相机合同。它们证明当前意图，但没有找到 U/MCVR 说明为何 bit 4 可从 fishing bobber 改为 priority。
- 风险/缺证：C `world.rgen` 与 U `9905` 的 primary/secondary mask 不等价；反射、shadow、detached camera、sleeping camera 的真实像素/遮挡待客户端验证。状态：`部分 / 静态已确认 / 运行待验`。

## M-07 透明、PBR、文本 alpha mode 与 glint 材质输入

- 两边路径符号：U `414d...:src/main/java/com/radiance/client/vertex/PBRVertexConsumer.java:75-155,181-444`、U `.../mixins/vulkan_render_integration/BlockModelRendererMixins.java`、U MCVR `9905...:src/common/shared.hpp:118-168`、`src/shader/util/{alpha_mode,vertex,text_mode}.glsl`、`src/shader/.../world/{default.rchit,transparent_only.rchit,transparent_only.rahit}`；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/client/vertex/PBRVertexConsumer.java:42-215,276-358,420-559`、C `.../mixins/vulkan_render_integration/{BlockModelRendererMixins,FluidRendererMixins,HandItemMaterialMixins,ItemRendererMixins}.java`、C `components/vulkan-renderer/src/common/shared.hpp:118-175`、C shader `src/shader/util/{alpha_mode,vertex,text_mode,glint_material}.glsl`、C `.../world/{default.rchit,transparent_only.rchit,transparent_only.rahit}`。
- 适用 Target：`1.21.1-neoforge`；两个内置 shader pack 的 world hit groups。
- 变化类型：Fabric `VertexConsumer`/RenderLayer API -> Mojang `set*`/RenderType API；alpha mode 与 PBR input 保留并扩展；text mode 重新编号到 12-19；glint 变成 material coating 语义。
- 行为直接证据：C `PBRVertexConsumer.java:45-61` 定义 opaque/cutout/transmission/coverage/additive 与八个 text mode；`:152-190` 按 RenderType 名称/transparency 选择 alpha mode；`:192-214` 将 NeoForge text layer 名称规范化；`:299-306` 把 alpha mode 写入 `PBR_POST_BASE` padding word；`:353-358` 在 block/entity transmission scope 下切换为 transmission。C `BlockModelRendererMixins.java:54-96` 读取 block emission 并写入 `PBRVertexConsumer.albedoEmission`；C `GlowItemFrameRendererMixins.java:27-82` 为 glow item frame 的 frame/map/item render scopes 注入 emission；C `SlimeOuterLayerMaterialMixins.java:15-30` 仅为可见 slime 外壳启用 entity transmission，避免 invisible outline 变成 dielectric；C `PBRVertexConsumer.java:469-471` 另有 `setColorMix`。C shared material layout `MaterialVertex` 保持 80 bytes 并包含 packedData/albedoEmission（C `shared.hpp:152-175`）。C `glint_material.glsl:4-18` 明确 glint 不改变 base alpha/transmission/shadow throughput，调整 `f0`/roughness 并返回 coating radiance；C `world/default.rchit:952-958` 采样 glint，`:963-970` 写入材质/发光路径。
- 原因证据：C `PBRVertexConsumer.java:51-53,174-179,421-425` 与 `glint_material.glsl:4-18` 是当前代码的语义注释；port 历史 `cd67362:SOURCE_FIRST_COMPATIBILITY_AUDIT.md:767-799,897-915` 记录 slime scope、name-tag material scope 和 glow-frame lightmap-to-emission 的设计原因。仍没有找到 U/MCVR 上游说明为何 text mode 从 1-8 移到 12-19，或为何 ordered alpha/transmission 采用当前 RenderType 名称判断。
- 风险/缺证：`RenderType` 名称、transparency state、Neoforge custom layer 和 material buffer 位布局必须与 shader 编译结果完全一致；静态字段/offsetof 不能证明 GPU 上的最终材质表现。状态：`接入 / 静态已确认 / 运行待验`。

## M-08 世界文字和 name tag：post-render -> priority geometry/background

- 两边路径符号：U `414d...:src/main/java/com/radiance/client/proxy/world/EntityProxy.java:131-197,315-364,636-683`、U `.../mixins/vulkan_render_integration/EntityRendererMixins.java:17-29`、U `.../constant/Constants.java:240-255`；U MCVR `9905...:src/shader/world/ray_tracing/internal/vanilla-pt/configs.json` 中 `post_render_text/name_tag` 与 `src/shader/world/ray_tracing/internal/vanilla-pt/post_render/*`；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/client/proxy/world/EntityProxy.java:366-448,451-535,1905-1925`、C `.../mixins/vulkan_render_integration/EntityRendererMixins.java:22-65`、C `.../client/vertex/PBRVertexConsumer.java:51-61,203-214`；C shader `.../vanilla-pt/priority/{background.rgen,priority.rgen,text.rahit,text.rchit,name_tag_text.rchit,outline.rchit,composite.comp}` 与 config `.../vanilla-pt/configs.json:381-399,588-594,894-1006`。
- 适用 Target：`1.21.1-neoforge`；当前 `vanilla-pt`/`advanced` 配置均有 priority pass（advanced 对应路径同名）。
- 变化类型：主动渲染行为改动；U 的 `PostRenderFlags.TEXT/NAME_TAG` 和 late raster post pass 改为 world capture + priority TLAS pass；see-through/name-tag 背景被分层。
- 行为直接证据：U `EntityProxy.java:315-334` 将 TextDisplay 捕获后 `processPostEntityRenderData(...TEXT)`；U `:337-364` 将 name tag 捕获后 `processPostEntityRenderData(...NAME_TAG)`；U MCVR config 以 `post_render/render_text.*` 等资源承载。C `EntityProxy.java:366-380` 将 TextDisplay normal 与 see-through RenderType 分到 world/priority providers；`:405-429` 分别以 `/text/default` 与 `/text/default/see_through` 加入实体数组；`:451-521` 将 name-tag background 和 normal glyph 放入 priority provider，并把 see-through glyph 送到明确 discarded provider（`:462-468`）。C `EntityRendererMixins.java:28-46` 把文本写入该 routing provider并拆背景；C `EntityProxy.java:1910-1918` 按层名识别 see-through/background。C priority config 在 `894-1006` 建立 `priority_background`、`priority_geometry`、`priority_composite`；C `priority/text.rchit:36-43` 与 `name_tag_text.rchit:36-40` 输出覆盖率/颜色，`composite.comp:13-16` 将优先层合成回 out image。
- 原因证据：C `EntityProxy.java:366-370`、`:462-468` 的注释给出“see-through 仍保留物理表面/priority post-world 负责最终顺序”“name-tag faint glyph 丢弃”的意图；port 历史 `cd67362:SOURCE_FIRST_COMPATIBILITY_AUDIT.md:600-627,767-799,801-813` 记录了 priority world-geometry、物理副本/优先副本和相机合同。该记录证明 port 设计意图，不是上游 414d/9905 的原生理由；name-tag see-through 丢弃是否与 vanilla 视觉合同一致仍待运行验证。
- 风险/缺证：这是当前最显著的主动渲染语义偏移之一；`postRenderFlag`/`EntityPost` native 结构仍存在，但当前 Java 路径均以 `processWorldEntityRenderData(..., post=false)` 入队。需要真实文本遮挡、透视、透明背景、远近叠加和 GUI 边界验证。状态：`部分 / 静态已确认 / 运行待验`。

## M-09 粒子：缓冲 sheet 与 CUSTOM nested capture

- 两边路径符号：U `414d...:src/main/java/com/radiance/client/proxy/world/EntityProxy.java:636-714`、U `.../mixins/vulkan_render_integration/ParticleManagerMixins.java`、U MCVR `9905...:src/shader/.../world/world.rgen`；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/client/proxy/world/EntityProxy.java:1004-1148`、C `.../mixins/vulkan_render_integration/{ParticleManagerMixins,ParticleMixins,CustomParticleBufferSourceMixins}.java`、C `components/vulkan-renderer/src/shader/.../world/world.rgen`。
- 适用 Target：`1.21.1-neoforge`。
- 变化类型：ParticleTextureSheet -> ParticleRenderType API；普通粒子由 post-render grouping 改为 world `PARTICLE` geometry；CUSTOM 增加 nested capture；内容名保留并增加 emission step。
- 行为直接证据：U `EntityProxy.java:640-683` 按 content name 聚合普通粒子并 `processPostEntityRenderData(...PARTICLE)`；U `:689-714` 单一 storage 捕获 CUSTOM。C `EntityProxy.java:1018-1029` 区分 NO_RENDER/缓冲类型/自定义类型；`:1030-1074` 按 content/render type/emission 建 provider，调用 particle.render 后以 `RayTracingFlags.PARTICLE` world geometry 入队；`:1086-1148` 对非缓冲类型创建 per-particle provider、DebugLine scope、stack，并显式 `storage.endBatch()`。C `ParticleManagerMixins.java:42-66` 给 particle 写 `/particle/<namespace>/<path>` content name；C `CustomParticleBufferSourceMixins.java:13-21` 将 ItemPickup/MobAppearance 的 `RenderBuffers.bufferSource()` 重定向到 capture provider。C shader `world.rgen` 在 primary/secondary mask 中包含 `PARTICLE_MASK`（如 `:440-448,471-485`）。
- 原因证据：C `EntityProxy.java:111-117` 说明 nested CUSTOM provider stack；`:1115-1118` 说明 endBatch 仅模拟 vanilla lifecycle；`:1151-1165` 说明 emission 是 particle light 相对环境 light 的差值；port 历史 `cd67362:SOURCE_FIRST_COMPATIBILITY_AUDIT.md:355-370` 记录 zero-alpha debug separator 与 particle world/emission 设计。该记录证明 port 设计意图，仍未找到上游 commit 解释普通粒子从 post-render 改为 world geometry，或 CUSTOM 特定类为何需要重定向。
- 风险/缺证：粒子 alpha/排序、CUSTOM renderer 的嵌套 `endBatch`、frustum 与 dynamic light 仍未运行验证；`PARTICLE_MASK` 参与 secondary/shadow 的具体可见性和光照与 U 的 `PostRenderFlags.PARTICLE` 不等价。状态：`部分 / 静态已确认 / 运行待验`。

## M-10 眼睛涂层：`eyes` layer -> base material emissive overlay

- 两边路径符号：U `414d...:src/main/java/com/radiance/client/proxy/world/EntityProxy.java:199-260`、U MCVR `9905...:src/common/shared.hpp:152-168`、`src/core/render/entities.cpp:59-76`、`src/shader/world/ray_tracing/internal/{advanced,vanilla-pt}/world/default.rchit`；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/client/proxy/world/EntityProxy.java:236-280`、C `components/vulkan-renderer/src/common/shared.hpp:152-175`、C `src/core/render/entities.cpp:55-152,181-200,520-536,1138-1170`、C `src/shader/util/emissive_overlay.glsl`、C `src/shader/world/ray_tracing/internal/{advanced,vanilla-pt}/world/{default.rchit,no_height.rchit}`。
- 适用 Target：`1.21.1-neoforge`；蜘蛛、洞穴蜘蛛、末影人等使用 vanilla `eyes` RenderType 的实体；两个内置 shader pack。
- 变化类型：native/material 本地新增；从独立 coplanar `eyes` geometry 改为与 base geometry 配对的发光纹理 coating；不是上游直接行为。
- 行为直接证据：U MCVR `9905` 的 `MaterialVertex` 末字段是 `pad0`，U `entities.cpp` 没有 eyes/overlay 配对，U shader 只按普通 default/any-hit 路由。C `entities.cpp:55-73` 排除不可作为 base 的语义 group；`:96-152` 只在 `geometryGroupNames == "eyes"`、纹理 ID 有效且位置/UV/indices 与另一 geometry 相同的条件下配对，给 base 写 `emissiveOverlayTextureID` 并移除冗余 eyes geometry；`:181-200` 把该 ID 写入 packed `MaterialVertex`。C `shared.hpp:152-168` 将原 padding word 改为 `emissiveOverlayTextureID`。C `default.rchit:973-984` 采样 overlay，并把其 RGB/alpha 作为 additive radiance；C advanced path 将 ID 编码到 first-hit cache（`advanced/primary/{default,no_height}.rchit:205-223`），再由 `advanced/common/direct_light_surface.glsl:123-128,239-247` 还原并采样。
- 原因证据：port 历史 `cd67362:SOURCE_FIRST_COMPATIBILITY_AUDIT.md:917-935` 记录了上游未注册 eyes group、1.21.1 Spider/CaveSpider 同表面重绘以及“只在 topology/position/UV 匹配时配对”的原因；当前源码本身也以 exact-surface 比较直接表达该安全边界。该历史记录证明 port 设计意图，不是上游 414d/9905 的原生理由。
- 风险/缺证：配对失败时当前仍保留 `eyes` geometry，但 shader config 未为 eyes 建专用 hit group，SBT 会走 default；配对成功时 eyes geometry 被移除，材质依赖 overlay texture sampling。纹理 alpha、PBR/lighting 与不同实体模型的真实结果未运行验证；不能把静态配对规则等同视觉通过。状态：`部分 / 静态已确认 / 运行待验`。

## M-11 Outline：vanilla outline target -> world line geometry / priority hit group

- 两边路径符号：U `414d...:src/main/java/com/radiance/client/vertex/StorageOutlineVertexConsumerProvider.java:13-95`、U `.../client/proxy/world/EntityProxy.java:262-310,716-779`、U `.../mixins/vulkan_render_integration/WorldRendererMixins.java:225-308`；U MCVR `9905...:src/shader/.../configs.json`（无 `priority_*` hit groups）；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/client/vertex/StorageOutlineVertexConsumerProvider.java:9-135`、C `client/render/WorldOutlineGeometry.java:15-42`、C `mixins/vulkan_render_integration/{ModelPartWorldOutlineMixins,WorldRendererMixins,GameRendererMixins}.java`、C `client/proxy/world/EntityProxy.java:324-338,433-442,526-535,686-825,1199-1292`、C shader `.../priority/{outline.rchit,priority.rgen,background.rgen,composite.comp}`。
- 适用 Target：`1.21.1-neoforge`；实体/方块实体 custom outline、ModelPart、方块选择 outline。
- 变化类型：主动渲染行为改动；U 的实体 outline provider 仅有待实现 TODO，C 增加实际 capture；vanilla framebuffer outline/transparency 初始化被取消，outline 作为 world/priority geometry 送入 TLAS。
- 行为直接证据：U `EntityProxy.java:290-301` 的实体 outline 分支注释为 TODO，未把 outline provider 接入 capture；U `:716-779` 仅构建 block selection lines，且高对比 secondary block outline 使用 U 的原始 mask 语义。C `WorldRendererMixins.java:129-137` 取消 `initOutline`/`initTransparency`；C `EntityProxy.java:324-338` 对 glowing/custom outline 建独立 storage provider，`:433-442,526-535` 以 `PRIORITY_OUTLINE_CONTENT` + `PRIORITY_ONLY` 送入 entity 数据；block entity 同样在 `:686-825` 处理。C `ModelPartWorldOutlineMixins.java:26-41` 将 ModelPart cubes 合成 `VoxelShape` 并发出边线；C `WorldOutlineGeometry.java:22-41` 对每条边写 position/color/normal。C config `vanilla-pt/configs.json:950-953` 与 advanced 对应段落把 `priority_outline` 绑定 `priority/outline.rchit`；该 shader `outline.rchit:7-12` 以固定 `vec4(vec3(4.0),1.0)` 命中。
- 原因证据：port 历史 `cd67362:SOURCE_FIRST_COMPATIBILITY_AUDIT.md:600-627,917-942` 记录 priority world-geometry redesign、ModelPart union、`VoxelShape.forAllEdges` 以及移除旧 star/text/outline post pass；当前 ModelPart helper 的注释与该记录一致。未见 U/MCVR 候选对该 redesign 的原生依据。
- 风险/缺证：C priority outline closest-hit 固定白色并忽略 vertex/team color；C entity outline 是 `PRIORITY_ONLY`，block selection 使用普通 WORLD debug lines，两者并非一个 mask。`initOutline`/`initTransparency` 取消后仍有 `GameRendererMixins` 的 post-effect hook，但没有客户端验证遮挡、透视、颜色、线宽、叠层或 outline 关闭开关。状态：`部分 / 静态已确认 / 运行待验`。

## M-12 字体与 glyph atlas：目标纹理 ID -> auxiliary atlas -> text hit any-hit

- 两边路径符号：U `414d...:src/main/java/com/radiance/mixins/vanilla_resource_tracker/{FontStorageMixins,GlyphAtlasTextureMixins,BitmapFontGlyphMixins,BuiltinEmptyGlyphMixins,TtfGlyphMixins,UnicodeTextureGlyphMixins,NativeImageMixins}.java`、U `.../client/texture/AuxiliaryTextures.java`；U MCVR `9905...:src/shader/util/text_mode.glsl`、`src/shader/.../world/text.rahit`；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/mixins/vanilla_resource_tracker/{FontStorageMixins,GlyphAtlasTextureMixins,BitmapFontGlyphMixins,BuiltinEmptyGlyphMixins,TtfGlyphMixins,UnicodeTextureGlyphMixins,NativeImageMixins}.java`、C `.../client/texture/AuxiliaryTextures.java`、C `components/vulkan-renderer/src/shader/util/text_mode.glsl`、`src/shader/.../world/{text.rahit,transparent_only.rahit}`。
- 适用 Target：`1.21.1-neoforge`；Bitmap/TrueType/Unihex glyph atlas 及 world/name-tag/TextDisplay text layers。
- 变化类型：Minecraft 1.21.4 Fabric font API -> 1.21.1 Mojang/NeoForge font API；纹理追踪/auxiliary upload 语义保留；动态 Unihex 路径有本地 upload/清空适配。
- 行为直接证据：U/C 两侧都在 FontSet/FontTexture（U FontStorage/GlyphAtlasTexture）之间插入 `IRenderableGlyphExt`，并让 glyph `upload(id,x,y)` 带 atlas target ID；C `FontStorageMixins.java:18-38`、`GlyphAtlasTextureMixins.java:27-46`、`BitmapFontGlyphMixins.java:50-91`、`TtfGlyphMixins.java:51-102` 直接可见。C `UnicodeTextureGlyphMixins.java:37-69,80-132` 通过 `TextureProxy.queueUpload` 上传 Unihex RGBA，并在同一 region 调用 `AuxiliaryTextures.clearDynamicRegion`。C `NativeImageMixins.java:47-70` 把 NativeImage upload 导向 `AuxiliaryTextures.loadAndUpload`；C `AuxiliaryTextures.java:188-195,244-269,392-407` 明确覆盖 `textures/font` 与动态 glyph 区域。C `text.rahit:59-79` 从 glyph atlas 纹理读取 coverage 并 stochastic ignore，不把文字误当 dielectric/transmission。
- 原因证据：U/C glyph mixin 的 `@reason to pass image targetID` 注释是接入目的；port 历史 `cd67362:SOURCE_FIRST_COMPATIBILITY_AUDIT.md` 中字体/文字章节记录了 atlas target 与 text coverage 语义。未找到上游候选对 1.21.1 API 迁移之外的额外理由。
- 风险/缺证：C access transformer `META-INF/accesstransformer.cfg:1-31` 与 U `radiance.accesswidener:1-75` 不是同一授权列表；动态字体、Unicode glyph reload、auxiliary atlas 清空和 text shadow coverage 未运行验证。状态：`接入 / 静态已确认 / 运行待验`。

## M-13 资源输入：Minecraft assets 保留，metadata/模块发生定向变化

- 两边路径符号：U `414d...:src/main/resources/**`；C `versions/1.21.1-neoforge/src/main/resources/**`，C `versions/1.21.1-neoforge/build.gradle.kts:577-590`。
- 适用 Target：`1.21.1-neoforge`；资源包路径本身由当前 Target 消费。
- 变化类型：资源树重定位；Fabric AW/mod metadata -> NeoForge AT/TOML；模块与语言删除星空参数；旧 loader metadata 保留但构建排除。
- 行为直接证据：对 U/C resource 相对路径和 SHA-256 的只读比较：`assets/minecraft` 共 337 文件全部路径与内容相同；全资源 C 356、U 354，公共相同 348、公共路径内容不同 5，C 额外 3，U 仅有而 C 尚未找到对应 1。不同公共文件为 `assets/radiance/lang/en_us.json`、`zh_cn.json`（删除 post-render star 四项）、`modules/post_render.yaml`（C `attributeConfigs: [ ]`，U 有 star_count/min/max/radius）、`radiance.mixins.json` 和保留的 `fabric.mod.json`（C 为 legacy/placeholder 文件，不是有效 NeoForge 入口）。C 额外文件为 `META-INF/accesstransformer.cfg`、`META-INF/mods.toml`、`META-INF/neoforge.mods.toml`；U 仅有而 C 尚未找到对应的是 `radiance.accesswidener`。C build `:586` 只 expand NeoForge TOML，`:589` 排除 `fabric.mod.json` 与 `META-INF/mods.toml`。
- 原因证据：C `AGENTS.md` 规定 NeoForge Target 以 AT/TOML 为权威且只有 1.21.1 实现；port 历史 `cd67362:SOURCE_FIRST_COMPATIBILITY_AUDIT.md:620-627` 记录 stars 移入 sky miss、移除旧 star/text/outline post resources。当前 post-render YAML/语言差异与该历史结论一致，但不是 upstream 候选的行为原因证明。
- 风险/缺证：资源 SHA 比较只证明当前工作树相对 U 候选的静态文件关系，不证明每个 1.21.1 原版纹理/atlas 兼容；旧 Fabric/mods TOML 文件虽被 processResources 排除，仍是树内可见输入，可能造成审计/工具误判。状态：`定位 / 部分 / 静态已确认 / 运行待验`。

## M-14 可选兼容隔离：Create/Flywheel/Ponder/Sable/Simulated/Veil/Aeronautics

- 两边路径符号：U `414d...:src/main/java/com/radiance/`（在候选 U 的该源码范围内尚未找到 `compatibility/**` 对应目录或兼容依赖）；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/compatibility/**`、C `.../mixins/compatibility/**`、C `versions/1.21.1-neoforge/src/main/resources/radiance.mixins.json:13-35`、C `versions/1.21.1-neoforge/target.properties:9-12`。
- 适用 Target：仅业务 `1.21.1-neoforge`；其他四治理节点没有这些类的业务 source set。
- 变化类型：相对 U/MCVR 候选的本地新增；NeoForge optional-mod compatibility 与 Sable/Simulated transformed-world bridge。
- 行为直接证据：C Mixin JSON `:13-35` 注册 23 个 compatibility mixin；C 这些类中大多数用 `@Pseudo`/字符串 target，例如 Create `CreateValueBoxMixins.java:12-46`、Flywheel `FlywheelVisualizationManagerMixins.java:12-21`、Sable `SableChunkedRenderDataDiagnosticsMixins.java:16-35`、Veil `VeilRenderSystemMixins.java:15-90`、Aeronautics `AeronauticsNeoForgeClientSetupMixins.java:22-48`。这些不是完整的兼容 backend：Flywheel `FlywheelProgramsReloaderMixins.java:9-18` 直接取消 reload；Veil `VeilRenderSystemMixins.java:35-39,41-71,73-90` 取消 OpenGL init/begin/end/renderPost/cleanup、把能力查询置 false、禁用 deferred lights。C `compatibility/sable/SableSubLevelBridge.java` 在 `EntityProxy`/`WorldRendererMixins` 中实际提供 transformed entity/block-outline/extra block queue；C `compatibility/create/CreateValueBoxWorldGeometry.java` 与 Simulated/Veil helpers 参与额外 world/UI routing。C `target.properties:9-12` 声明 Create/Sable/Aeronautics/Veil 版本；profile inputs 位于 `validations/compatibility-{create,sable,aeronautics}/task/profile.json`，而 clean profile 的 `compatibilityMods` 为空。
- 原因证据：兼容类注释与 `@Pseudo` 说明其目标是避免缺少可选 mod 时直接绑定；但 C `mixin_related/MixinPlugin.java:19-21` 的 `shouldApplyMixin` 只返回全局 `ENABLED`，没有按 mod ID 条件筛选，当前 target/build 的 compileOnly 与 profile 分离是接入证据而非运行隔离证明。未在 U/MCVR 候选找到这些兼容功能的上游理由或等价路径。
- 风险/缺证：Mixin JSON 仍在 clean 客户端注册全部兼容 mixin；大多数有 `@Pseudo`，但部分 Simulated/Veil mixin 直接引用目标类型，是否在完全无兼容 mod 的真实 classloading 下安全未验证。已确认的 hook/skip 不应表述为“兼容功能已恢复”：Sable transformed world、Create additional geometry、Veil stage/event 与核心 world capture 的组合仍未运行验证，Veil 也明确只保留跳过 OpenGL 路径。状态：`接入 / 部分 / 静态已确认 / 运行待验`。

## M-15 JNI/native/shader 可达链与 hitgroup 解析

- 两边路径符号：U `414d...:src/main/java/com/radiance/client/proxy/world/{ChunkProxy,EntityProxy}.java` -> U MCVR `9905...:src/core/middleware/com_radiance_client_proxy_world_{ChunkProxy,EntityProxy}.cpp` -> U `src/core/render/{chunks,entities}.cpp` -> U `src/core/render/modules/world/ray_tracing/submodules/world_prepare.cpp` -> U shader configs；C `versions/1.21.1-neoforge/src/main/java/io/github/recrivenvi/radiance/client/proxy/world/{ChunkProxy,EntityProxy}.java` -> C `components/vulkan-renderer/src/core/middleware/io_github_recrivenvi_radiance_client_proxy_world_{ChunkProxy,EntityProxy}.cpp` -> C `src/core/render/{chunks,entities}.cpp` -> C `src/core/render/modules/world/ray_tracing/submodules/world_prepare.cpp`/`ray_tracing_module.cpp` -> C `src/shader/world/ray_tracing/internal/{advanced,vanilla-pt}/configs.json`。
- 适用 Target：`1.21.1-neoforge`；当前 native target config `components/vulkan-renderer/cmake/targets/1.21.1-neoforge.cmake`。
- 变化类型：JNI namespace/path relocation；world geometry group names are carried into SBT; current component retains MCVR batch/BLAS/TLAS architecture and adds priority/eyes material paths.
- 行为直接证据：C `ChunkProxy.java:613-625` 与 `EntityProxy.java:1881-1903` 声明 native array contracts；C middleware Chunk `:32-64`、Entity `:7-63` 将 pointers 组成 `ChunkBuildTask`/`EntitiesBuildTask`。C `entities.cpp:472-520` 读取 geometry/content/group，`:323-362` 建实体 BLAS；C `GameRendererMixins.java:175-183` 在 `renderLevel` 尾部调用 `EntityProxy.build()` 后调用 `RendererProxy.fuseWorld()`，C native `...RendererProxy.cpp:220-228` 将其转为当前 pipeline context 的 `fuseWorld()`。C `world_prepare.cpp:152-192` 调度 chunk/entity BLAS，`:195-265` 为每个实体建立 TLAS instance 并按 `geometryGroupNames` 填 `hitGroupNames`，`:328-375` 追加 chunk group；`:434-454` 把 `hitGroupNames` 映射到当前 pass 的 SBT index。C `ray_tracing_module.cpp:998-1043` 从 config 建 closest/any/intersection shader group 名称映射，`:1124-1208` 组装 Vulkan RT pipeline。C `vanilla-pt/configs.json:893-1006` 与 advanced 对应段落声明 priority background/geometry/composite 及 `text`、`text_see_through`、`priority_outline` hit groups；C ordinary world config `:745-847` 绑定 `world/text.rahit`、`world/transparent_only.*`，world shader 使用 same TLAS group names。
- 原因证据：U MCVR `9905` `world_prepare.cpp:253-264,340-347` 已直接证明“geometry group name -> SBT hitgroup”是上游架构；C `priority`/`emissiveOverlay` 的额外语义由 port 历史 `SOURCE_FIRST_COMPATIBILITY_AUDIT.md:600-627,917-935` 记录。没有 runtime trace/RenderDoc 证据，本项只确认静态可达。
- 风险/缺证：`world_prepare` 对未知 group 使用 `fallbackHitGroupIndex`；这只是静态 fallback 选择，不能据此宣称无异常或行为安全，也不保证期望的 emission/outline/text hitgroup。`prebuiltBLAS >= 0` 路径当前仍显式抛 `prebuilt blas not implemented yet!`（C `world_prepare.cpp:246-255`），且 U MCVR `9905` 同一位置也存在该 inherited unimplemented branch，因此它不是本地新增丢失。JNI header hash、native CTest、shader compilation、SBT runtime dispatch由主任务负责，本子任务未执行。状态：`接入 / 静态已确认 / 运行待验`。

## M-16 当前仍需保留的行为差异与审计残留

- 两边路径符号：U `414d...`/U MCVR `9905...` 对应上述各项；C 当前实际路径均以 `versions/1.21.1-neoforge/**` 与 `components/vulkan-renderer/**` 为准。
- 适用 Target：当前唯一业务 Target；不向四个治理节点外推。
- 变化类型：残留/未知分类，不把差异数量当能力缺失。
- 行为直接证据：已直接确认的主动偏移包括：M-06 bit 4 从 `FISHING_BOBBER_MASK` 改为 `PRIORITY_MASK`；M-08 旧 post-render text/name-tag 路由改为 physical + priority geometry；M-09 ordinary particle/weather/priority 组合改变；M-10 eyes overlay composition；M-11 outline world/priority path；M-13 stars/post-render resource 定向删除；M-14 optional compatibility mixins。已定位但未穷尽语义的 API 改名包括 RenderType/FontTexture/SectionCompiler/LevelRenderer、VertexConsumer setter names、NeoForge client hooks、AT/TOML。
- 原因证据：部分原因来自当前代码注释或 port 历史审计（各项已标注）；其余主动渲染偏移保持“原因证据未知”，不做推断。
- 风险/缺证：本记录没有启动游戏/GUI、没有构建或 CTest、没有调用真实 JNI、没有运行任何 target；因此所有“运行待验”必须由后续真实客户端/用户视觉验收重新确认。覆盖以能力单元 M-01..M-16 计，不按文件数量或删除数宣布完成。状态：`部分 / 静态已确认 / 运行待验`。
