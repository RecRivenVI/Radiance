# MC-04.2 implementation checkpoint

Root applied the accepted default decision: general DebugRenderer output and LevelRenderer section debug geometry keep their existing colors/geometry and now use emission0. F3+G ChunkBorderRenderer is scoped separately by a WrapMethod and routed to emission1 thin/thick buffers; F3+B entity-hitbox capture and ordinary target-block outline code were not changed.

Source path check: Minecraft1.21.1 DebugRenderer.render invokes ChunkBorderRenderer then GameTest debug; LevelRenderer.renderDebug emits sectionPath/sectionVisibility data, not F3+G. Therefore setting all DebugRenderer buffers to0 without separate ChunkBorder scope would have violated the named F3+G decision; the new scope prevents that.

Modified: EntityProxy.queueDebugGeometry, WorldRendererMixins levelDebugStorage; added DebugEmissionScope/ChunkBorderEmissionMixins and one exception/nesting scope regression test; registered mixin. Current Java compile/test pending next unified window. No visual run.
