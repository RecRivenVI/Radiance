# Additional descriptor bindings isolation
Start from descriptor-stages/before/ui_module.cpp (ALL_GRAPHICS retained). Remove set0 bindings2..5 and all 8 initialization/refresh writes to them. Keep bindings0/1 plus dynamic UBO set1; keep no-op and table generation. Skip creation of DIAGRAM, SPIDER, SPIDER_BLUR_TEMP_V15/V7 post pipelines requiring removed slots. postSpider/postDiagram throw explicit diagnostic errors if reached. Title test only; dependent-effect errors invalidate comparison. This necessary companion restriction changes pipeline compilation workload; success cannot attribute cause solely to binding count. Same traces-off/validation-off/early-window-on, fixed b619 input. No shader translator changes. All temporary edits archived and will be restored after testing.

Result: Release build and packaged verification passed. PID16964 real client AV after27.780608s. TitleScreen text ShaderProxy.draw -> nvoglv64.dll+0xf1c729, reading0x0000002000000000. This is the previously seen alternate invalid-read variant, not the read-1 signature. No dependent-effect guard message in console, no validation DLL in hs_err, cache b619 selected. The title path remains reachable with additional descriptor slots removed. Binary hashes and loaded native runtime path saved in hs_err/experimental-hashes.csv.
Both layout trials failed: stage-only34.883538s/read-1; binding-only27.780608s/read0x2000000000. Each was separate from the same baseline, not a combined upstream-layout rollback; do not claim all descriptor changes excluded. The binding trial additionally skips dependent post pipeline creation, so workload differs. No evidence here that either restriction fixes the issue.
Restored source ui_module.cpp, deployed core.dll/core.lib and instance mods/Radiance.jar from descriptor-stages/before. Verified exact source hash and deployed DLL/JAR hashes. Keeps bind-noop and lifecycle diagnostics source, leaves earlyWindowControl=true; traces not persisted. Build output DLL/PDB and Gradle outputs remain experimental until next rebuild; do not blindly redeploy them. Archive includes matching experimental DLL/PDB/JAR for both runs. No staged changes/commits/push. Suggested next isolated candidate: dynamic UI pipeline creation via PipelineCache + creation feedback, compared with direct vkCreateGraphicsPipelines(VK_NULL_HANDLE); do not delete caches.
