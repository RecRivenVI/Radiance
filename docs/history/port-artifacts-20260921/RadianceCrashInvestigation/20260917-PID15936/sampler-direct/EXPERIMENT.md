# Built-in sampler wrapper isolation
Only ShaderTranslator built-in translate() path bypasses RadianceSampler2D and texture/texelFetch wrappers. External stages retain existing behavior. Retain 0x7fffffff ID decoding to avoid interpreting framebuffer flip flag as an array index; flip-Y behavior is intentionally disabled for built-ins in this temporary diagnostic. Uniform layout/IDs and native DLL unchanged. earlyWindowControl=true. No native/Java trace or validation. Existing bind-noop retained. Same b619 input cache preferred; original timestamp restored after run. Generated shader content changes its cache key automatically; do not delete caches.

Result: built and packaged successfully, real client PID61624 crashed after32.437141s; TitleScreen text -> ShaderProxy.draw -> nvoglv64.dll+0xf1c729, read0xffffffffffffffff. Same signature. The new generated text shader key309327024773fb4b28016360 contains direct texture macros and no RadianceSampler2D wrapper, proving the built-in experiment took effect. Uniform declarations unchanged. DLL unchanged; experimental JAR hash captured. Selected cache b619 confirmed in console. No validation DLL in hs_err. No trace enabled. Single run only.
Inference: bypassing the built-in sampler wrapper is insufficient to prevent the title crash. This weakens it as a necessary cause; does not prove every shader/compiler interaction is harmless. Do not retain a feature regression as a fix.
Restored ONLY this trial's ShaderTranslator.java from before/ and deployed instance mods/Radiance.jar from before/. Exact source hash and DLL/JAR baseline hashes verified. Experimental source, patch, JAR, generated shaders, console and hs_err archived here. Build outputs still represent the experimental build until the next normal Gradle packaging run (which sees restored source); instance mod JAR is baseline. earlyWindowControl=true and bind-noop remain. No commit/staging/push.
