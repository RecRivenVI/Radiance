# Dynamic UI pipeline direct creation
Only DynamicGraphicsPipelineBuilder::build changes device->createGraphicsPipelines(...) to vkCreateGraphicsPipelines(device->vkDevice(),VK_NULL_HANDLE,...). pipelineCreateInfo.pNext is zero-initialized and never assigned here, so no creation-feedback structure. Keep all layout/sampler/generation code at pretrial baseline. Other graphics/compute/ray pipelines retain PipelineCache and its feedback wrapper; same b619 cache input preferred for those. Early window=true, Java/native trace and validation disabled. Do not delete caches. Rebuild native from restored source, do not reuse previous experimental build output. Snapshot source and deployed DLL/LIB/JAR; matching experiment PDB to be archived.

## Build-control correction
First launch PID28852 was INVALID for isolated attribution: restoration by Copy-Item preserved an old ui_module.cpp timestamp, so incremental MSBuild reused ui_module.obj from the prior binding-removal trial. Detected by build log and source/object timestamps. Archived entire first run plus matching DLL/JAR/PDB under invalid-incremental-run; it crashed before the normal-close attempt. Not counted as the one-variable result.
Touched restored ui_module.cpp (content unchanged, includes ALL_GRAPHICS and bindings2..5), rebuilt successfully; build-corrected.log explicitly shows ui_module.cpp compilation. Then redeployed corrected DLL and repackaged for valid run. Native dynamic_pipeline.cpp was compiled in initial build, and only its direct creation change remains in the corrected experimental source. All earlier temporary shader/layout changes are absent.

## Valid result
PID54760: real-client EXCEPTION_ACCESS_VIOLATION after32.526263s; TitleScreen text ShaderProxy.draw -> nvoglv64.dll+0xf1c729, read0xffffffffffffffff. Same baseline crash signature. Builds and distribution verification passed. No tracing/validation enabled, no validation DLL in hs_err. Cache b619 still used by other unchanged pipeline builders, expected; dynamic builder bypasses it with VK_NULL_HANDLE and null pNext. Loaded native hash recorded against actual runtime path in hs_err. DLL/JAR/PDB/source/patch/logs archived.
Conclusion: bypassing BOTH dynamic UI cache and feedback wrapper is insufficient to prevent crash. Does not exclude caches/feedback on other pipeline types, descriptor mutation or command-state handling. No proof of fix or root cause.

## Restored state
Restored exact pretrial dynamic_pipeline.cpp source content, source resource DLL/LIB and deployed instance JAR; hashes verified. Touched restored source timestamp and successfully rebuilt baseline native source (build-restored.log shows dynamic_pipeline.cpp) so native object outputs no longer contain experiment. Rebuilt baseline native outputs were NOT redeployed: instance retains original baseline BF6E... DLL /2F79... JAR. Gradle packaged outputs remain from the experiment until next normal preparePackagedClient; do not bypass packaging with those. Early window=true; bind-noop remains. No source changes from this experiment remain, no staged/committed/pushed changes.
