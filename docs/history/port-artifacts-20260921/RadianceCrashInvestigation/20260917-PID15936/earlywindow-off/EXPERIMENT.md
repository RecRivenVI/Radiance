Single launch: earlyWindowControl=false, existing DLL/JAR unchanged, skip distributedJar/preparePackagedClient. Native and Java trace disabled; explicit validation layer disabled. RTSS environment left unchanged. Same pipeline cache b619 preferred; timestamp restored after launch. No source changes. Config false retained for user review.

Result: crash reproduced. PID48812, elapsed21.166750s. hs_err reports EXCEPTION_ACCESS_VIOLATION reading0xffffffffffffffff at nvoglv64.dll+0xf1c729, TitleScreen text -> ShaderProxy.draw. Same signature as prior title crash. Gradle final exit -1073740791 is recorded separately from the hs_err AV signature. Bootstrap console explicitly confirms earlyWindowControl=false; same cache b619 loaded. DLL/JAR hashes unchanged before/after. Loaded runtime core.dll path matches lifecycle build; no Khronos validation DLL listed in hs_err; RTSS remains unchanged. No world entry. Only one launch performed. earlyWindowControl remains false; before config saved in fml.before.toml. Disabling early window is insufficient to avoid the crash and weakens the early-window rendering/handoff path as a necessary trigger; shared runtime initialization/rendering changes are not excluded.
